export type ContactHeroContentModel = {
  id: string;
  eyebrow: string;
  headline: string;
  copy: string;
  primaryCta: {
    label: string;
    href: string;
  };
  background: {
    image: string;
  };
};
